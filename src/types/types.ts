export interface ContactInfo {
    name: string;
    phone: string;
    email: string;
}

export interface LeadInfo {
    name: string;
    email: string;
    loanAmount: string;
    incomeSource: string;
    incomeAmount: string;
    termsAccepted: boolean;
}